import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:['https://image.tmdb.org','in.bmscdn.com','assets-in.bmscdn.com']
  }
};

export default nextConfig;
